const initState = {
    projects: [
        {id: '1', title: 'help me find a dude', content: 'test me'}
    ]
};

const projectReducer = (state = initState, action) => {
return state;
}

export default projectReducer;